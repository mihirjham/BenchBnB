(function(root) {
  'use strict';

  var FilterParamsConstants = root.FilterParamsConstants = {
    UPDATE_BOUNDS: "UPDATE_BOUNDS",
    UPDATE_MIN_SEATING: "UPDATE_MIN_SEATING",
    UPDATE_MAX_SEATING: "UPDATE_MAX_SEATING"
  };
}(this));
